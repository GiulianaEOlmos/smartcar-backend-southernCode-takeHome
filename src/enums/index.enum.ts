export enum EngineCommand {
  START = "START_VEHICLE",
  STOP = "STOP_VEHICLE",
}

export enum ActionStatus {
  EXECUTED = "EXECUTED",
  FAILED = "FAILED",
}

export enum SmartCarStatus {
  SUCCESS = "success",
  ERROR = "error",
}

export enum SmartCarAction {
  START = "START",
  STOP = "STOP",
}
